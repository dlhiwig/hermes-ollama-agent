# Delegation Protocol

Use this when a request benefits from multiple specialist passes.

1. Plan: split objective into independent subtasks with clear outputs.
2. Assign: map each subtask to a specialist role.
3. Execute: run role workers in parallel when tasks do not block each other.
4. Integrate: combine outputs, resolve contradictions, and order next actions.
5. Verify: define concrete checks before completion.
